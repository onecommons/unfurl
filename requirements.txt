# XXX sync with setup.py (use pbr)
# Versions are pinned to prevent pypi releases arbitrarily breaking
# tests with new APIs/semantics. We want to update versions deliberately.
ansible==2.5.5
click==6.7
six==1.10.0
enum34
jsonschema
subprocess32
ruamel.yaml>=0.15.37

boto==2.34.0
pyOpenSSL==16.2.0
shade==1.24.0
passlib==1.6.5

-r tosca-parser/requirements.txt
