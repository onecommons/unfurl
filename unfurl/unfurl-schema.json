{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "unfurl.yaml",
  "description": "Unfurl configuration file",
  "type": "object",
  "properties": {
    "unfurl": {
      "type": "object",
      "properties": {
        "version": { "$ref": "./manifest-schema.json#/definitions/version" }
      },
      "required": ["version"]
    },
    "manifests": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "file": { "type": "string" },
          "default": { "type": "boolean" },
          "context": {"type": "string"}
        },
        "required": ["file"]
      }
    },
    "contexts": {
      "type": "object",
      "allOf": [
        { "$ref": "./manifest-schema.json#/definitions/namedObjects" },
        {
          "additionalProperties": {
            "$ref": "./manifest-schema.json#/definitions/context"
          }
        }
      ]
    }
  },
  "required": ["unfurl"]
}
